import { Settings } from 'lucide-react'
import { PagePlaceholder } from '@/components/layout/PagePlaceholder'

export function ConfiguracoesPage() {
  return (
    <PagePlaceholder
      title="Configurações"
      description="As configurações do sistema serão implementadas em uma sprint futura."
      icon={Settings}
    />
  )
}

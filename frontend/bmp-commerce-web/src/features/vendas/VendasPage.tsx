import { ShoppingCart } from 'lucide-react'
import { PagePlaceholder } from '@/components/layout/PagePlaceholder'

export function VendasPage() {
  return (
    <PagePlaceholder
      title="Vendas"
      description="O módulo de vendas será implementado em uma sprint futura."
      icon={ShoppingCart}
    />
  )
}

import { Package } from 'lucide-react'
import { PagePlaceholder } from '@/components/layout/PagePlaceholder'

export function ProdutosPage() {
  return (
    <PagePlaceholder
      title="Produtos"
      description="O cadastro de produtos será implementado em uma sprint futura."
      icon={Package}
    />
  )
}
